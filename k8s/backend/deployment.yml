apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: miniapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: helenhaveloh/k8s_backend:v1         # eigenes Image verwenden, wenn auf Rechner liegt, sonst Docker Hub!
        imagePullPolicy: IfNotPresent
        envFrom:
          - configMapRef:
              name: app-config
          - secretRef:
              name: app-secret
        ports:
          - containerPort: 3000